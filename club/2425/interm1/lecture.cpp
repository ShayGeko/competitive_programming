#include <bits/stdc++.h>

using namespace std;

int main() {
    /*
        integers: 0, 5, -10, 256, 1e9
        int x; <= 1000000000 ~ 2^31 

        long long ~ 2^63
        long long y;

        char c; 'c', '0';

        int arr[] = {1, 2, 3, 4};
        strings str; str.length. str.reverse 

        float f; double d;
        long double dd;

        bool b; true/false
    */
    // ios_base::sync_with_stdio(0);
    // cin.tie(0);cout.tie(0);
    // int n, y, x;
    // string a;
    // cin >> n >> a >> x;

    // cout << n << " "<< a << " " << x << '\n';

    // string str;
    // getline(cin, str);

    // string total = "";
    // while(cin >> str) {
    //     total += str;
    // }
    // cout << total << '\n';

    // int arr[] = {1, 2, 3, 4, 5};

    /*
        each testcase starts with n, 

        5 1 2 3 4 5
        2 1 2
        4 1 2 3 4
        EOF
    */

    // int n;
    // while(cin >> n) {
    //     // process the testcase
    //     vector<int> nums(n);
    //     for(int i = 0; i < n; ++i) cin >> nums[i];

    //     auto ans = f(nums);
    //     cout << ans << '\n';
    // }

    // for (int i = 0; i < n; i++)
    // for(int i = n; i>= 0; i--)

    /*
        int i = 0;
        while(i < n) {
            // do stuff

            ++i;
        }

        do {
        
        } while()


    */

   /*
    vector<int> my_vec = {5,4,3,2,1};
    for(int &x : my_vec) {
        cout << x << '\n';


        x = 2;
    }

    int n;
    cin >> n;
    vector<int> nums(n);
    for(auto &x : nums) cin >> x;
   
   */

    double d = 1. / 3;

    cout << fixed << setprecision(10) <<  d << '\n';

    return 0;
}