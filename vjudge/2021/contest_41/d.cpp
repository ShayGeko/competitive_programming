#include <bits/stdc++.h>

using namespace std;

const int maxn = 1e5;
const int INF = 1e9;
typedef struct{
    int x;
    int y;
} vertex;
vertex v[maxn];
vector<int> adj[maxn];
int main(){
    ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);

    int t;
    cin >> t;
    while(t--) {
        int n;
        int k;
        // either all adjacent, or impossible
    }
    return 0;
}