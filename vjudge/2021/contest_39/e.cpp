#include <bits/stdc++.h>

using namespace std;

const int maxn = 1e5;

int main(){
    ios_base::sync_with_stdio(false);cin.tie(0);cout.tie(0);

    // build matrix A, n^2 * n^2, where i = # of element in 2d array
    // A[i][j] = 1 if it increases j when we do someth on i
    // solve Ax = [invmod arr[i]]
    return 0;
}